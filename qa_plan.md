# מדריך תוכנית בדיקות ואימות (QA Testing & Checklist Plan)

מסמך זה מרכז את תרחישי הבדיקה הנדרשים לאימות תקינות פעילות המערכת, כולל בדיקות לפרונטאנד, לבקאנד, ולאימות התיקונים הטכנולוגיים שבוצעו במנוע הבוט.

---

## 1. בדיקות אימות אבטחה ואישור (Security & Auth Verification)

### תרחיש א': קריאה חיצונית לקרון ללא מפתח (Cron Call No Auth)
* **יעד:** `/api/cron/run`
* **פעולה:** שליחת בקשת `POST` או `GET` ללא כותרות (Headers) בכלל.
* **תוצאה מצופה:**
  * סטטוס קוד: `401 Unauthorized`
  * גוף תשובה (JSON): `{"success": false, "error": "Unauthorized: cron_secret not configured"}` (במידה והמפתח אינו מוגדר) או `{"success": false, "error": "Unauthorized"}`.

### תרחיש ב': קריאה חיצונית לקרון עם מפתח שגוי (Cron Call Invalid Token)
* **יעד:** `/api/cron/run`
* **פעולה:** שליחת בקשה עם כותרת `Authorization: Bearer wrong_secret_key`.
* **תוצאה מצופה:**
  * סטטוס קוד: `401 Unauthorized`
  * גוף תשובה: `{"success": false, "error": "Unauthorized"}`

### תרחיש ג': קריאה חיצונית לקרון עם מפתח תקין (Cron Call Valid Token)
* **יעד:** `/api/cron/run`
* **פעולה:** שליחת בקשה עם כותרת `Authorization: Bearer <CRON_SECRET>` (כפי שמוגדר ב-env או ב-DB).
* **תוצאה מצופה:**
  * סטטוס קוד: `200 OK`
  * גוף תשובה: JSON המכיל את סטטיסטיקת ההרצה (`success: true`, `scanResult`, `generatedCount`, `publishedCount`).

---

## 2. בדיקות מנוע סריקה ומונטיזציה (Discovery & Monetization Test)

### תרחיש א': סריקת מוצרים שכשלו במונטיזציה (Failed Monetization Link)
* **פעולה:** הרצת סריקה (ידנית או דרך ה-Cron) כאשר מנוע יצירת הקישורים השותפים נכשל (לדוגמה, עקב שגיאת API של AliExpress או בעיית רשת).
* **תוצאה מצופה:**
  * המערכת רושמת אזהרה/שגיאה בדוח הריצה (`errors`).
  * **חשוב:** המוצר **לא** נשמר בבסיס הנתונים ו**לא** מועבר לתור במצב `pending`. הוא מסונן החוצה לחלוטין כדי למנוע פרסום מוצרים ללא עמלה.

### תרחיש ב': סריקת מוצרים תקינים (Successful Monetization)
* **פעולה:** הרצת סריקה תחת תנאים רגילים.
* **תוצאה מצופה:**
  * מוצרים חדשים שעוברים את כל הפילטרים (עמלה, פידבק, הנחה, כמות מכירות) נוצרים בהצלחה בטבלאות המסד תחת סטטוס `pending`.
  * נוצר קישור שותפים תקין בעמודה `affiliateLink`.

---

## 3. בדיקות מניעת Race Condition ופרסום כפול (Optimistic Locking Test)

### תרחיש א': קריאות חופפות לפרסום (Concurrent Scheduler Execution)
* **הסבר:** הדמיה של הרצה חופפת של שני תהליכי Cron בו-זמנית (למשל, קריאה מה-Cron האוטומטי ובו-זמנית לחיצה ידנית על כפתור הסריקה והפרסום בפרונט).
* **בדיקת קוד (קוד המקור ב-`scheduler.ts`):**
  * ודא שקיים שימוש ב-`updateMany` שמתנה את שינוי הסטטוס ל-`publishing` בכך שהסטטוס הישן עדיין תואם לסטטוס שנקרא (`pending` או `publish_failed`).
* **תוצאה מצופה:**
  * רק תהליך אחד יצליח לעדכן את הסטטוס ל-`publishing` (ויקבל `count: 1`).
  * התהליך השני יקבל `count: 0`, ידלג על הפרסום של המוצר הנוכחי (`continue`), ולא יפרסם אותו שוב לערוץ הטלגרם.

---

## 4. בדיקת השבתת המערכת (Bot Shutdown Support)

### תרחיש א': השבתת בוט מהדשבורד (bot_active = false)
* **פעולה:** היכנס ללשונית "כללי סינון וסריקה" בהגדרות והשבת את הבוט (או כבה בדשבורד הראשי).
* **פעולה 2:** שלח קריאה ידנית ל-`/api/cron/run` כדי לדמות קרון אוטומטי.
* **תוצאה מצופה:**
  * סריקת מוצרים חדשים לא תניב תוצאות (מנוע הסריקה מדווח כי הבוט כבוי).
  * **חשוב:** שלב ה-AI (יצירת תוכן) ושלב ה-Telegram (פרסום מוצרים מהתור) **נעצרים לחלוטין** ולא רצים כלל.
  * השרת מחזיר תגובת הצלחה ריקה ללא עיבוד נוסף.

---

## 5. בדיקת פולבאק של פרסום תמונות לטלגרם (Telegram Photo Fallback Test)

### תרחיש א': כישלון טלגרם בטעינת תמונה (Broken/Heavy Image Fallback)
* **פעולה:** הגדר מוצר עם קישור תמונה שאינו נגיש לשרתי טלגרם (לדוגמה, תמונה משרת לוקאלי או תמונה שבורה). אשר את פרסומו.
* **תוצאה מצופה:**
  * הקריאה הראשונית ל-`sendPhoto` תיכשל עם שגיאה.
  * המערכת תבצע פולבאק אוטומטי ותשלח קריאה ל-`sendMessage` (טקסט בלבד) עם קישור מוטמע בתחילת ההודעה `[📸](URL)`.
  * **חשוב:** הקישור המוטמע (`photoLink`) יישלח באופן תקין ולא יישבר. סימנים כמו נקודות (`.`) ומקפים (`-`) בתוך ה-URL של התמונה **אינם** מבוטלים עם בקסלאש (`\`), כך שהתמונה נטענת בהצלחה בתצוגה המקדימה של טלגרם.
